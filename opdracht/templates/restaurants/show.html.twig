{% extends 'base.html.twig' %}

{% block title %}Restaurants{% endblock %}

{% block body %}
    <h1>Restaurants</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ restaurant.Naam }}</td>
            </tr>
            <tr>
                <th>TelefoonNummer</th>
                <td>{{ restaurant.TelefoonNummer }}</td>
            </tr>
            <tr>
                <th>Plaats</th>
                <td>{{ restaurant.Plaats }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('restaurants_index') }}">back to list</a>

    <a href="{{ path('restaurants_edit', {'id': restaurant.id}) }}">edit</a>

    {{ include('restaurants/_delete_form.html.twig') }}
{% endblock %}
