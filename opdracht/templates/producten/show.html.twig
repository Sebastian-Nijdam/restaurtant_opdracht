{% extends 'base.html.twig' %}

{% block title %}Producten{% endblock %}

{% block body %}
    <h1>Producten</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producten.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ producten.Naam }}</td>
            </tr>
            <tr>
                <th>Voorraad</th>
                <td>{{ producten.Voorraad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producten_index') }}">back to list</a>

    <a href="{{ path('producten_edit', {'id': producten.id}) }}">edit</a>

    {{ include('producten/_delete_form.html.twig') }}
{% endblock %}
